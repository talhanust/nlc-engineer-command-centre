## What & why

<!-- What feature area does this PR port/add? Link the matching prototype screen. -->

## Parity

<!-- How does this match the prototype's behavior? Note any intentional deviation. -->

## Checklist

- [ ] One feature area; small and reviewable
- [ ] `npm run typecheck` clean
- [ ] `npm test` passes (and reproduces the relevant prototype assertions)
- [ ] Both builds pass (`VITE_DATA_MODE=local` and `=api`)
- [ ] No client name (FGEHA, etc.) used as global branding
- [ ] Colors via theme tokens; strings externalized; keyboard accessible
- [ ] No secrets or real data added
