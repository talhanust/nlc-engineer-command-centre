{
  "name": "nlc-engineer-command-centre",
  "version": "0.1.0",
  "private": true,
  "description": "NLC Engineer Command Centre — construction & financial project-control platform for National Logistic Corporation (HQ NLC → HQ Engineers → PD HQs → projects).",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace @nlc-ecc/web run dev",
    "build": "npm --workspace @nlc-ecc/web run build",
    "test": "npm --workspace @nlc-ecc/web run test",
    "typecheck": "npm --workspace @nlc-ecc/web run typecheck"
  }
}
